# Production Type
IsDev="true or false"

# Application Secrets
FrontendHost="host:port"
PublicHost="host"
PublicPort="port"
RedirectHost="domain" # change this to click.xyz in prod
RedirectPort="port"

# DB Config
DBHost=host
DBPort=port
DBUser=user
DBPassword=password
DBName=name

# Goose Configuration
GOOSE_DRIVER="postgres"
GOOSE_DBSTRING=postgres://user:password@host:port/name
GOOSE_MIGRATION_DIR=./migrations

# OPENAI KEYS
OPENAI_KEY="Key String"

# Cloudmersive API
CLOUDMERSIVE_KEY="Key String"
